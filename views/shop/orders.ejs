<%- include('../includes/head.ejs') %>
</head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <h1>Orders !</h1>
        <ol>
            <% for(let order of orders) { console.log(order) %>
                
                    <li>Quantity <%= order._id%></li>
                    <ul>
                    <% for(let product of order.products) { %>                       
                        <li>
                            <span><%= product.productData.title%> Price <%= product.productData.price%> Quantity <%= product.quantity%></span>
                        </li>                                               
                    <% } %>
                    </ul>
            <% } %>
         </ol>
       
    </body>

<%- include('../includes/end.ejs') %>